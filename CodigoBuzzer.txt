//clase para el buzzer
class Buzzer{
  public:
  Buzzer(byte _pin){
    pin = _pin;
    pinMode(8, OUTPUT);
  }
  bool ActivarBuzzer(bool activar){
    if(activar){
      tone(pin, 700);
     	
    }else{
      noTone(pin);
     
     
    }
  }
  private:
  byte pin;

};